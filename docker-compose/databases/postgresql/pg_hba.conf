# PostgreSQL Client Authentication Configuration File

# Local Unix socket
local   all   postgres   peer

# Docker networks - SSL not required
host    all   all        172.19.0.0/16   scram-sha-256

# External connections - SSL + client certificate required
hostssl all   all        0.0.0.0/0       cert map=certmap
